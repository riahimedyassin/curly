name: "React Component"
description: "Modern React functional component with TypeScript"
version: "1.0.0"

conventions:
  - component:
      - naming: "pascalCase"
  - hook:
      - naming: "camelCase"

variables:
  componentName:
    type: "string"
    required: true
    description: "Component name in PascalCase"

  componentPath:
    type: "string"
    default: "./src/components"
    description: "Path where component will be generated"

  includeTests:
    type: "boolean"
    default: false
    description: "Generate test file"

  includeProps:
    type: "boolean"
    default: true
    description: "Include props interface"

  styling:
    type: "string"
    default: "none"
    description: "Styling approach"

  exportType:
    type: "string"
    default: "default"
    description: "Export type"

files:
  - source: "Component.tsx.tmpl"
    destination: "{{ .ComponentPath }}/{{ .ComponentName }}/{{ .ComponentName }}.tsx"
    name: "component"

  - source: "index.ts.tmpl"
    destination: "{{ .ComponentPath }}/{{ .ComponentName }}/index.ts"
    name: "index"

  - source: "Component.test.tsx.tmpl"
    destination: "{{ .ComponentPath }}/{{ .ComponentName }}/{{ .ComponentName }}.test.tsx"
    condition: "{{ .IncludeTests }}"
    name: "test"
